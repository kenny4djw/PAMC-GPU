[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -ra -q
markers =
    slow: slow-running tests (e.g. Boltzmann sampling statistical tests)
filterwarnings =
    ignore::DeprecationWarning
