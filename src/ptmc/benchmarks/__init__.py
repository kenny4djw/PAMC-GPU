"""Physical benchmarks (stage 8): reduced coarse-grained models that probe the
energy model's qualitative physics (electrostatic orientation switching;
hydrophobic bistability). Network access to the PDB is unavailable in this
environment, so full atomistic lysozyme / protein-G-B1 structures cannot be
fetched; these reduced models exercise the SAME energy model and pipeline and
would accept parsed atomistic structures unchanged.
"""
